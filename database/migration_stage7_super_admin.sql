-- ============================================================================
-- Stage 7 — מנהל כללי (super admin)
-- ----------------------------------------------------------------------------
-- מוסיף דגל is_super_admin למשתמשים. מנהל כללי רואה ומנקד את כל התלמידים;
-- מנהל רגיל רק את התלמידים המשויכים אליו (assigned_admin_id).
-- ה-root (OA000000000) מסומן אוטומטית כמנהל כללי.
-- ============================================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS is_super_admin BOOLEAN NOT NULL DEFAULT FALSE;

-- סמן את המנהל הראשי הקיים כמנהל כללי
UPDATE users SET is_super_admin = TRUE WHERE username = 'OA000000000';
