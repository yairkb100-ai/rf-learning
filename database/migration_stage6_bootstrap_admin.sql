-- ============================================================================
-- Stage 6 — מנהל "ממלא מקום" (bootstrap admin)
-- ----------------------------------------------------------------------------
-- מטרה: על שרת חדש טבלת users ריקה, ולכן אין למי להתחבר כדי להקים את המערכת.
-- מיגרציה זו יוצרת מנהל זמני אחד — אך ורק אם אין עדיין אף מנהל (ADMIN) במערכת.
--
--   username: OA000000000
--   סיסמה:   Admin123   (זמנית! חובה להחליף — ראה DEPLOY.md)
--
-- הזרימה המיועדת:
--   1. מתחברים עם המנהל הזמני.
--   2. מקימים מנהל OA אמיתי משלכם.
--   3. מוחקים את המנהל הזמני מהדשבורד.
--
-- בטיחות: ה-WHERE NOT EXISTS מבטיח שעל בסיס נתונים קיים (שכבר יש בו מנהל)
-- המיגרציה לא עושה כלום — אין כפילות ואין דריסה.
-- ============================================================================

INSERT INTO users (full_name, national_id, username, password_hash, role, title)
SELECT
  'מנהל ראשי (זמני)',
  '000000000',
  'OA000000000',
  -- bcrypt(Admin123, 10)
  '$2a$10$Q5nqPIj8kC0qjZkKUEvAHOyboPt6rWi9qBoJ9GVHcSnBW9Rq3oX9.',
  'ADMIN',
  'מנהל מערכת'
WHERE NOT EXISTS (
  SELECT 1 FROM users WHERE role = 'ADMIN'
)
-- הגנה נוספת: לא ליצור אם ה-username או ת"ז הזמניים כבר תפוסים (מונע התנגשות)
AND NOT EXISTS (
  SELECT 1 FROM users WHERE username = 'OA000000000' OR national_id = '000000000'
);
